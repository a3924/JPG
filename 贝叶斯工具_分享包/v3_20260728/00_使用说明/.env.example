# 给使用方参考的示例文件：可作为模板，但请把下面的占位符替换成你自己在
# GitHub → Settings → Developer settings → Personal access tokens 里生成的值。
# 推荐只授予 public_repo 权限 (fine-grained token)，不要勾选任何不需要的 scope。
# 生成后复制到本目录的 .env 文件中，并删除本 .env.example，避免误提交。
GITHUB_PAT=__在这里填你本人的PAT__
